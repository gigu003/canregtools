% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_site_morp.R
\name{create_site_morp}
\alias{create_site_morp}
\alias{create_site_morp.canreg}
\alias{create_site_morp.canregs}
\alias{create_site_morp.fbswicds}
\alias{create_site_morp.fbswicd}
\title{Count ICD-10 site and morphology groups by strata}
\usage{
create_site_morp(
  x,
  ...,
  type = c("icd10", "morp", "both"),
  flatten = TRUE,
  collapse = TRUE,
  wrap_subsite = FALSE,
  class_morp = TRUE,
  drop_nos = TRUE,
  cancer_type = "big"
)

\method{create_site_morp}{canreg}(
  x,
  ...,
  type = c("icd10", "morp", "both"),
  flatten = TRUE,
  wrap_subsite = FALSE,
  class_morp = TRUE,
  drop_nos = TRUE,
  cancer_type = "big"
)

\method{create_site_morp}{canregs}(
  x,
  ...,
  type = c("icd10", "morp", "both"),
  flatten = TRUE,
  collapse = TRUE,
  wrap_subsite = FALSE,
  class_morp = TRUE,
  drop_nos = TRUE,
  cancer_type = "big"
)

\method{create_site_morp}{fbswicds}(
  x,
  ...,
  type = c("icd10", "morp", "both"),
  flatten = TRUE,
  collapse = TRUE,
  wrap_subsite = FALSE,
  class_morp = TRUE,
  drop_nos = TRUE
)

\method{create_site_morp}{fbswicd}(
  x,
  ...,
  type = c("icd10", "morp", "both"),
  flatten = TRUE,
  wrap_subsite = FALSE,
  class_morp = TRUE,
  drop_nos = TRUE
)
}
\arguments{
\item{x}{An object with class \code{canreg}, \code{canregs}, \code{fbswicd}, or \code{fbswicds}.}

\item{...}{Variables used to define strata. For example, \code{year}, \code{sex}, and
\code{cancer} will return frequencies within each combination of year, sex, and
cancer group.}

\item{type}{Character. Type of frequency table to return when
\code{flatten = TRUE}. Options are \code{"icd10"}, \code{"morp"}, and \code{"both"}.
\code{"icd10"} returns ICD-10 site-code frequencies and is the default;
\code{"morp"} returns morphology-code or morphology-class frequencies;
\code{"both"} returns both results as a named list with elements \code{icd10} and
\code{morp}.}

\item{flatten}{Logical. If \code{TRUE}, flatten the \code{icd10} or \code{morp} list-column
frequency tables in a single \code{fbswicd} object into ordinary data frames
with one row per stratum-code combination. If \code{FALSE}, keep the
intermediate list-column structure with one row per stratum and two
list-columns, \code{icd10} and \code{morp}.}

\item{collapse}{Logical. Only used for \code{canregs} and \code{fbswicds}. If \code{TRUE},
merge multiple registry objects before creating site/morphology frequency
tables. If \code{FALSE}, return one result per registry object as a list.}

\item{wrap_subsite}{Logical. If \code{TRUE}, collapse ICD-10 subsite codes into
two broad groups: codes ending with \code{.9} are coded as \code{"CXX.9"}; all other
subsite codes are coded as \code{"CXX.0"}. Counts are then summed within these
broad groups.}

\item{class_morp}{Logical. If \code{TRUE}, classify morphology codes with
\code{\link[=classify_morp]{classify_morp()}} before summing counts. If \code{FALSE}, counts are returned
by the original morphology code. When \code{cancer} is included in the
stratification variables, its value is used as the \code{group} argument passed
to \code{\link[=classify_morp]{classify_morp()}}, currently \code{"60"} or \code{"125"}.}

\item{drop_nos}{Logical. Reserved for future use. Currently this argument is
accepted but not used by the function.}

\item{cancer_type}{Character. Cancer classification type passed to
\code{\link[=count_canreg]{count_canreg()}} when \code{x} is a \code{canreg} or \code{canregs} object.}
}
\value{
The return value depends on input class, \code{flatten}, \code{collapse}, and \code{type}:
\itemize{
\item For a single \code{fbswicd} or \code{canreg} object with \code{flatten = TRUE} and
\code{type = "icd10"}, returns a data frame with the stratification variables,
\code{icd10}, and \code{count}.
\item With \code{flatten = TRUE} and \code{type = "morp"}, returns a data frame with the
stratification variables, \code{morp}, and \code{count}.
\item With \code{flatten = TRUE} and \code{type = "both"}, returns a named list containing
\code{icd10} and \code{morp} data frames.
\item With \code{flatten = FALSE}, returns a data frame with the stratification
variables, \code{icd10}, and \code{morp}, where \code{icd10} and \code{morp} are list-columns
of frequency tables.
\item For \code{canregs} or \code{fbswicds}, \code{collapse = TRUE} returns a merged result;
\code{collapse = FALSE} returns one result per registry object as a list.
}
}
\description{
\code{create_site_morp()} counts ICD-10 site codes or morphology groups within
user-defined strata. It can be applied to objects with class \code{canreg},
\code{canregs}, \code{fbswicd}, or \code{fbswicds}.
}
\details{
For \code{canreg} and \code{canregs} objects, the function first calls
\code{\link[=count_canreg]{count_canreg()}} to create \code{fbswicd}/\code{fbswicds} objects, and then counts the
\code{icd10} and/or \code{morp} frequency tables stored in the \code{sitemorp} element.
}
\examples{
\dontrun{
data("canregs")

create_site_morp(canregs, year, sex, cancer)
create_site_morp(canregs, year, sex, cancer, type = "morp")
create_site_morp(canregs, year, sex, cancer, type = "both")
create_site_morp(canregs, year, sex, cancer, collapse = FALSE)

fbsw <- count_canreg(canregs[[1]])
create_site_morp(fbsw, year, sex, cancer, flatten = FALSE)
}
}
