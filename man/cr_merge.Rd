% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cr_merge.R
\name{cr_merge}
\alias{cr_merge}
\alias{cr_merge.canregs}
\alias{cr_merge.fbswicds}
\alias{cr_merge.asrs}
\alias{cr_merge.qualities}
\alias{cr_merge.age_rates}
\alias{cr_merge.summaries}
\title{Merge elements from objects of class \code{canregs}, \code{fbswicds}, or \code{asrs}}
\usage{
cr_merge(data)

\method{cr_merge}{canregs}(data)

\method{cr_merge}{fbswicds}(data)

\method{cr_merge}{asrs}(data)

\method{cr_merge}{qualities}(data)

\method{cr_merge}{age_rates}(data)

\method{cr_merge}{summaries}(data)
}
\arguments{
\item{data}{An object with class of \code{canregs}, \code{fbswicds}, \code{asrs},
\code{qualities}, \code{age_rates}, or \code{summaries}.}
}
\value{
An object with merged elements.
}
\description{
Merge elements from object with class of \code{canregs}, \code{fbswicds}, \code{asrs},
\code{qualities}, \code{age_rates}, or \code{summaries} into object with class of \code{canreg},
\code{fbswicd}, \code{asr}, \code{quality}, \code{age_rate}, or \code{summary}.
}
\examples{
data("canregs")
canreg <- cr_merge(canregs)
class(canreg)

# Merge obejct with class of `fbswicds` into obejct with class of `fbswicd`
fbsws <- count_canreg(canregs)
fbsw <- cr_merge(fbsws)

# Merge obejct with class of `asrs` into object with class of `asr`
asrs <- create_asr(canregs, year, sex, cancer, collapse = FALSE)
asr <- cr_merge(asrs)

# Merge obejct with class of `qualities` into object with class of `quality`
quas <- create_quality(canregs, year, sex, cancer, collapse = FALSE)
qua <- cr_merge(quas)

# Merge obejct with class of `age_rates` into object with class of `age_rate`
agerates <- create_age_rate(canregs, year, sex, cancer, collapse = FALSE)
agerate <- cr_merge(agerates)

# Merge obejct with class of `summaries` into object with class of `summary`
summs <- summary(canregs, collapse = FALSE)
summ <- cr_merge(summs)

}
