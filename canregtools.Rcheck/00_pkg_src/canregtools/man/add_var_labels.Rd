% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_labels.R
\name{add_var_labels}
\alias{add_var_labels}
\title{Add Variable Labels with Units}
\usage{
add_var_labels(x, label_type = "abbr", lang = "cn", break_line = TRUE)
}
\arguments{
\item{x}{A character vector of variable codes to be labeled (e.g., \code{"cr"}, \code{"asr"}, \code{"prop"}).}

\item{label_type}{Label style: \code{"abbr"} (default) for abbreviation, \code{"full"} for full name.}

\item{lang}{Language for the labels. One of \code{"cn"} (default) for Chinese, \code{"en"} for English.
Special options \code{"code"} or \code{"icd10"} return the variable code or ICD-10 code instead.}

\item{break_line}{Logical. If \code{TRUE} (default), adds a line break (\verb{\\\\n}) before the unit.
If \code{FALSE}, appends the unit directly after the label, suitable for HTML rendering
(e.g., in \code{flextable}).}
}
\value{
A character vector of the same length as \code{x}, where each element is a formatted label.
}
\description{
Returns formatted labels (with optional units) for commonly used
statistical variables in cancer registry reporting, such as incidence,
mortality, crude rate, age-standardized rate, and proportion.
}
\details{
This function looks up the corresponding label for each input variable code
in the internal dictionary \code{tidy_var_maps[["stats"]]}, and optionally adds
a unit suffix (e.g., \verb{(1/10<sup>5</sup>)} or \verb{(\%)}) depending on the variable type.
It supports Chinese and English, full names or abbreviations, and can format
the unit with or without a line break.
}
\examples{
add_var_labels(c("cr", "asr"))
add_var_labels(c("cr", "prop"), label_type = "full", lang = "en")
add_var_labels("asr", break_line = FALSE)

}
