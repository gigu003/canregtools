% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esti_fbswicd.R
\name{esti_fbswicd}
\alias{esti_fbswicd}
\title{Estimate object of fbswicd}
\usage{
esti_fbswicd(obj, pop = NULL)
}
\arguments{
\item{obj}{An object with class of \code{fbswicd}.}

\item{pop}{An population dataset to override the population in \code{obj}.}
}
\value{
An estimated object with class of \code{fbswicd}.
}
\description{
This function estimates \code{fbswicd} object based on existing \code{fbswicd} object
and using the total population data.
}
\details{
The estimation proceeds in the following steps:
\enumerate{
\item Calculate age-specific incidence and mortality rates for different
sex and cancer sites.
\item Derive estimated counts of \code{fbs} and \code{sws} using the
rate × population formula.
\item Recalculate proportions (e.g., \code{mv}, \code{dco}) back to
estimated case counts.
\item Join all information and return a new object of the same class as \code{obj}.
}
}
\seealso{
\code{\link[=create_age_rate]{create_age_rate()}}, \code{\link[=cr_filter]{cr_filter()}}
}
