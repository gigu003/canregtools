% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canreg_report.R
\name{create_canreg_report_data}
\alias{create_canreg_report_data}
\title{Create a standardized cancer registry report data object}
\usage{
create_canreg_report_data(
  data,
  year,
  province = NULL,
  province_en = NULL,
  population_total = NULL,
  summary_codes = c(all = "410000", urban = "910000", rural = "920000"),
  cancer_codes = c(101:110, 112, 114:118, 120:125),
  cancer_total_code = 60,
  cancer_type = "big",
  keep_source = TRUE,
  include_age_registry = TRUE
)
}
\arguments{
\item{data}{An object of class \code{canregs} or \code{fbswicds}.}

\item{year}{Data year stored as metadata. The statistical calculations still
use the \code{year} column in the input data as a grouping variable.}

\item{province, province_en}{Province names stored as metadata.}

\item{population_total}{Optional total provincial population stored as
metadata.}

\item{summary_codes}{Named character vector identifying the summary groups.
Defaults to \code{c(all = "410000", urban = "910000", rural = "920000")}.}

\item{cancer_codes}{Cancer codes to store as metadata for report generation.}

\item{cancer_total_code}{Code for all malignant cancers. Defaults to \code{60}.}

\item{cancer_type}{Cancer classification type passed to \code{\link[=count_canreg]{count_canreg()}} when
\code{data} is a \code{canregs} object.}

\item{keep_source}{Logical. If \code{TRUE}, keep intermediate \code{fbswicd} objects in
\code{source}.}

\item{include_age_registry}{Logical. If \code{TRUE}, calculate registry-level
age-specific incidence and mortality rates.}
}
\value{
An object of class \code{canreg_report}.
}
\description{
\code{create_canreg_report_data()} builds a compact report-ready object from
\code{canregs} or \code{fbswicds} data. The object stores summary-level indicators
for the province/all areas, urban areas, and rural areas, as well as
registry-level indicators for individual registries.
}
\examples{
\dontrun{
data("canregs")
report_data <- create_canreg_report_data(canregs, year = 2022)
summary(report_data)
}
}
